# Usage

```
make gen // get data generator
make input // get input
make clean // clean executive files and input/output files.
```


```
./bloomFilter -M 0 // naive method
./bloomFilter -M 1 // unordered_multiset
./bloomFilter -M 2 -N [bitNum] -K [hashNum] // bloomFilter
```
